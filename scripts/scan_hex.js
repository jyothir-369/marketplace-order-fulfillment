const fs=require('fs');const path=require('path');const root=process.argv[2];const exts=process.argv[3].split(',');const re=new RegExp('#[0-9a-fA-F]{3}([0-9a-fA-F]{3})?\\b');function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory()){if(e.name==='node_modules'||e.name==='.next'||e.name==='dist')continue;walk(p);}else if(exts.some(x=>e.name.endsWith(x))){const c=fs.readFileSync(p,'utf8');const lines=c.split('\\n');lines.forEach((l,i)=>{if(re.test(l)&&!l.trim().startsWith('*'))console.log(p+':'+(i+1)+':'+l.trim().slice(0,140));});}}}walk(root);